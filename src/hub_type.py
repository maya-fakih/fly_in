"""Created hub type enum to know start hub from final one."""
from enum import Enum


class HubType(Enum):
    """Justt being extra and creating hub types lol."""

    start = 0
    end = 1
    regular = 2
